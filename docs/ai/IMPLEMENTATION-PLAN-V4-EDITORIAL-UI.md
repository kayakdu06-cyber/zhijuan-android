# 实施计划：V4 编辑式界面纵切片

日期：2026-08-21  
实现 commit/分支：无（项目按用户要求不使用 Git）  
负责人/智能体：Codex  

## 范围

- 需求 ID：PV-010、PV-011、PV-016；保持 PV-002、PV-004、PV-005、PV-009、PV-012、PV-013 的既有入口与状态动作。
- 本次用户可见结果：将已确认的“现代手稿 / 排字编辑”视觉落到书库、创作工作台、章节目录、阅读器和底部导航；所有数据继续来自现有本地项目。
- 明确不做：不修改 Provider、提示词、模型调用次数、生成协调器、文件格式、持久化实体、路由数量；不增加语音、云同步、富文本或自动连写。
- 预计有效时长：单个 UI 纵切片，完成后立即运行现有 JVM、仪器与 APK 构建门禁。

## 最小上下文

- 已读文档：第三版 `AGENTS.md`、`START_HERE.md`、`docs/00-index.md`、`docs/02-interaction.md`、`docs/03-architecture.md`、视觉系统 `MASTER.md`。
- 已读契约：`constraints.json`、`requirements.json`、`routes.json`。
- 需要保持的不变量：3 模块、4 路由、3 个底部入口、单活动任务、一次一章、普通章节恰好两次调用、正文先保存、PendingCommit 幂等提交、结算摘要不进入正文界面。

## 纵向步骤

| # | 跨模块结果 | 预计改动文件 | 测试/证据 | 完成条件 | 状态 |
|---|---|---|---|---|---|
| 1 | 主题令牌和原生组件统一，业务状态不变 | `S0Theme.kt`、新增编辑式组件文件、drawable | Debug 编译 | 浅/深色令牌、48dp 触控和 3 项底栏可用 | complete |
| 2 | 书库仍从 Repository 快照展示并保留创建、继续、导入导出、删除 | `S5TextProjectUi.kt` | `S5TextProjectUiTest` | 多项目、恢复状态与保护删除仍可达 | complete |
| 3 | 生成页仍驱动现有 Coordinator/前台服务，并增加同页目录面板 | `S0App.kt`、编辑式组件文件 | JVM + 仪器测试 | 各持久化阶段仍映射到唯一主要操作 | complete |
| 4 | 阅读器仍读取章节文件、恢复位置、切章并隐藏结算摘要 | `S0App.kt` | `S0ReaderScreenTest` | 正文优先、目录可达、摘要不可见 | complete |
| 5 | 整体回归与视觉对照 | 测试、报告、截图 | 51 JVM、相关仪器测试、Debug APK、同视口截图 | 无 P0/P1/P2 视觉或交互回归 | complete |

## 风险与故障路径

- 将注入的失败：复用现有恢复测试；本切片不新增业务失败类型。
- 恢复后的权威状态：仍由现有 Repository、GenerationJobStore 与 PendingCommit 决定，Compose 不持有权威状态。
- 是否涉及额外模型调用：否。
- 是否新增依赖/路由/模块/持久化实体：否。
- Image 2 使用边界：只有参考图中出现无法用既有 Logo、Material 矢量图标或 Compose 排版表达的栅格素材时才生成；当前视觉目标没有此类素材。

## 完成核对

- [x] 关联测试通过
- [x] 需求—测试追溯更新
- [x] 无密钥/正文敏感日志
- [x] 进程重建路径验证
- [x] 未修改权威规格，无需运行规格仓库 `docs_lint.py`
- [x] 有可复核证据而非口头“完成”
