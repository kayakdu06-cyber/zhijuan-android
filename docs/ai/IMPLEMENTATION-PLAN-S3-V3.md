# 实施计划：V3-S3 可用界面、前台任务与恢复

日期：2026-08-20  
实现 commit/分支：NONE（项目不使用版本控制）  
负责人/智能体：Codex

## 范围

- 需求 ID：`PV-005`、`PV-006`、`PV-008`、`PV-009`、`PV-010`、`PV-011`、`PV-012`、`PV-013`、`PV-016`、`PV-017`。
- 用户可见结果：长时单章生成由前台任务承载；进程重建后能够识别草稿/待结算/待提交并给出明确恢复操作；阅读字体、行距与主题可调且本机保存。
- 已提前完成：四个顶级路由骨架、章节目录、上下章、摘要隐藏。
- 明确不做：不自动重跑 POST、不自动连写、不新增模型调用、不做复杂大纲编辑器、不引入 WorkManager/Room。

## 纵向步骤

| # | 跨模块结果 | 完成条件 | 状态 |
|---|---|---|---|
| 1 | 审计活动任务、草稿与 PendingCommit 的磁盘事实 | 状态矩阵和现有差异写入节点报告 | complete |
| 2 | 前台单章任务与通知/取消 | 同一时刻只运行一个任务；通知可回到 App；用户取消不提交未知结果 | complete |
| 3 | 恢复协调器 | PendingCommit 自动幂等应用；已有正文只结算、不重复正文调用 | complete |
| 4 | 阅读设置 | 字号、行距、浅色/深色/跟随系统持久化；目录继续可用 | complete |
| 5 | 仪器、进程重建、真机与 release 验收 | 节点报告 COMPLETE 并自动进入 S4 | complete |

## 保持的不变量

`modules=3` `routes=4` `provider_protocols=1` `active_jobs<=1` `chapters_per_job=1` `normal_calls=2` `POST_auto_retry=0` `Room=forbidden`。
